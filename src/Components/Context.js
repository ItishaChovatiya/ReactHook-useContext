import { createContext } from "react";

// Creating a context named dataContext
const dataContext = createContext();

// Exporting dataContext to be used in other components
export default dataContext;
